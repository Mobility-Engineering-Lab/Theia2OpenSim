name: theia2opensim
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip>=24
  - numpy>=1.26,<3
  - pandas>=2.2,<3
  - ipykernel
  - jupyterlab
  - pip
  - pip:
      - ezc3d>=1.7.0
      - pyomeca>=2024.0.2

# Optional dependency for OpenSim Python API is not included by default.
# Install manually when needed based on your platform/distribution.
